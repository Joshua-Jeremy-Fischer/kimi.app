use_default_settings:
  engines:
    # duckduckgo: häufig CAPTCHA bei Server-IP → nur Google/Bing
    keep_only:
      - google
      - bing

general:
  instance_name: "KimiSearch"
  debug: false

server:
  port: 8080
  bind_address: "0.0.0.0"
  secret_key: "kimi-searxng-secret-42x"
  limiter: false
  public_instance: false

search:
  safe_search: 0
  default_lang: "de-DE"
  formats:
    - html
    - json

outgoing:
  request_timeout: 10.0
  max_request_timeout: 15.0
