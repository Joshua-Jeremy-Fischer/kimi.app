# Kimi-Stack ohne eigenen cloudflared — Tunnel „esovai-mcp“.
# Öffentlich: https://kamikimi.esovai.tech → nur kimi-frontend:80 (Nginx proxy’t /api + /health → kimi-backend:3010).
# Nach docker compose up: nur kimi-frontend ins Netz des MCP-cloudflared hängen, z. B.:
#   docker network connect <NETZNAME> kimi-frontend

services:
  backend:
    build: ./backend
    container_name: kimi-backend
    restart: unless-stopped
    env_file: .env
    cap_drop: [ALL]
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=10m,mode=1777
    environment:
      DB_PATH: /data/kimi.db
    volumes:
      - kimi_data:/data
    # Wazuh läuft in einem separaten Stack → über Host-IP erreichbar
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app-net

  frontend:
    build:
      context: ./frontend
      args:
        ALLOWED_TOKEN: ${ALLOWED_TOKEN}   # aus .env → in nginx.conf eingebettet
    container_name: kimi-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "127.0.0.1:3009:80"
    read_only: true
    cap_drop: [ALL]
    # Port 80 < 1024: ohne NET_BIND_SERVICE startet nginx mit cap_drop ALL nicht zuverlässig
    cap_add: [NET_BIND_SERVICE, CHOWN, SETUID, SETGID]
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=10m,mode=1777
      - /var/cache/nginx:size=50m,mode=1777
      - /var/run:size=1m,mode=0755
    networks:
      - app-net


  mobile-frontend:
    build:
      context: ./mobile-frontend
      args:
        ALLOWED_TOKEN: ${ALLOWED_TOKEN}
    container_name: kimi-mobile
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "127.0.0.1:3012:80"
    read_only: true
    cap_drop: [ALL]
    cap_add: [NET_BIND_SERVICE, CHOWN, SETUID, SETGID]
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=10m,mode=1777
      - /var/cache/nginx:size=50m,mode=1777
      - /var/run:size=1m,mode=0755
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app-net

  ollama:
    image: ollama/ollama
    container_name: ollama
    restart: unless-stopped
    env_file: .env
    cap_drop: [ALL]
    security_opt:
      - no-new-privileges:true
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - app-net

  searxng:
    image: searxng/searxng:latest
    container_name: kimi-searxng
    restart: unless-stopped
    environment:
      - SEARXNG_BASE_URL=http://kimi-searxng:8080
    volumes:
      - ./searxng/settings.yml:/etc/searxng/settings.yml
    networks:
      - app-net

  eso-bot:
    build: ./eso-bot
    container_name: eso-bot
    restart: unless-stopped
    env_file: .env
    environment:
      AGENT_PORT: "3020"
      SANDBOX_ROOT: /workspace
      # Defaults (kannst du in .env überschreiben)
      ALLOW_SHELL: "${ALLOW_SHELL:-false}"
      ALLOW_WEB: "${ALLOW_WEB:-true}"
      ALLOW_FILES: "${ALLOW_FILES:-true}"
      ALLOW_GIT: "${ALLOW_GIT:-false}"
    volumes:
      - eso_workspace:/workspace
    cap_drop: [ALL]
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=10m,mode=1777
    networks:
      - app-net

  openclaw-gateway:
    image: ghcr.io/openclaw/openclaw:latest
    container_name: openclaw-gateway
    restart: unless-stopped
    environment:
      # OpenClaw → lokales Ollama
      OLLAMA_BASE_URL: "http://ollama:11434"
      OLLAMA_API_KEY: ""
      # Security-Header deaktivieren damit nginx-Proxy iframing erlauben kann
      DISABLE_FRAME_OPTIONS: "true"
      DISABLE_CSP: "true"
    ports:
      - "127.0.0.1:18789:3000"
    cap_drop: [ALL]
    cap_add: [NET_BIND_SERVICE]
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=10m,mode=1777
    networks:
      - app-net
    depends_on:
      - ollama

  langchain-agent:
    build: ./langchain-agent
    container_name: langchain-agent
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: "8001"
      ESO_BOT_BASE_URL: "http://eso-bot:3020"
    cap_drop: [ALL]
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=10m,mode=1777
    networks:
      - app-net
    depends_on:
      - eso-bot

networks:
  app-net:
    driver: bridge

volumes:
  ollama_data:
  kimi_data:
  eso_workspace:
